# dotfiles
My dotfiles.

Neovim configuration lives in `nvim/` (formerly `nvim-exp/`). Install it with
`stow nvim`. When upgrading an existing checkout, unlink `nvim-exp` before
updating, then run `stow nvim` to replace the old symlinks.
